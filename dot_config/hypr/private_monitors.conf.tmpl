{{- /*
Machine-specific Hyprland monitor configuration.

Rendered destination:
  ~/.config/hypr/monitors.conf

Expected source file:
  .chezmoitemplates/hypr/monitors/<hostname>.conf

Check hostname with:
  chezmoi execute-template '{{ .chezmoi.hostname }}'
*/ -}}

{{- $monitorConfig := printf "hypr/monitors/%s.conf" .chezmoi.hostname -}}
{{- includeTemplate $monitorConfig . -}}
