# espanso match file

################################################################################
#======================== TEMPLATE FOR NEW CLIENTS =============================
################################################################################

# # ==============================================================================
# # Imaginary Domain
# # ==============================================================================
# - regex: ';credid(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
#   vars:
#     - name: client_config
#       type: echo
#       params: { echo: "filn|imaginarydomain.org|" } # EDIT THIS LINE
#     - *credential_output
#     - *temporary_password
#   replace: "{{credentials}}\n{{temppass}}"

################################################################################

# ==============================================================================
# CREDENTIAL MATCHES
#
# Each client uses one client_config value:
#   username_format|email_domain|optional_email_local_format
#
# Examples:
#   fnln|client.org|          = johnsmith / johnsmith@client.org
#   fnli|client.org|fn.ln     = johns / john.smith@client.org
#
# Leave the third field blank to use the username format for the email address.
# This is the standard use, but optionally allows the email username to be
# overwritten.
#
# Format shortcuts:
#   fn    = firstname only                    (john)
#   ln    = lastname only                     (smith)
#   fnln  = firstname + lastname              (johnsmith)
#   fnli  = firstname + last initial          (johns)
#   filn  = first initial + lastname          (jsmith)
#   fili  = first initial + last initial      (js)
#   fn.ln = firstname + "." + lastname        (john.smith)
#   fn.li = firstname + "." + last initial    (john.s)
#   fi.ln = first initial + "." + lastname    (j.smith)
#   fi.li = first initial + "." + last initial (j.s)
#
# To add a client, copy an entry below and change only:
#   1. Client name comment
#   2. regex trigger line
#   3. client_config value
# ==============================================================================

anchors:
  credential_output: &credential_output
    name: credentials
    type: script
    params:
      args:
        - sh
        - "%CONFIG%/scripts/credential-generator.sh"

  temporary_password: &temporary_password
    name: temppass
    type: match
    params:
      trigger: ";tp"

matches:
  # ==============================================================================
  # Alaska Family Services
  # ==============================================================================
  - regex: ';credafs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "fnli|akafs.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Alaska SeaLife Center
  # ==============================================================================
  - regex: ';credaslc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "fnli|alaskasealife.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Anchorage Neurosurgical Associates, Inc.
  # ==============================================================================
  - regex: ';credanai(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|akneurosurgery.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Camp Fire Alaska
  # ==============================================================================
  - regex: ';credcfa(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|campfireak.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # CCS Early Learning
  # ==============================================================================
  - regex: ';credccs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|ccsalaska.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Cross Road Medical Center
  # ==============================================================================
  - regex: ';credcrmc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|crossroadmc.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Girl Scouts of Alaska
  # ==============================================================================
  - regex: ';credgsak(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|girlscoutsalaska.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Indian Family Health Clinic
  # ==============================================================================
  - regex: ';credifhc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "fn.ln|ifhcgf.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Junior Achievement of Alaska
  # ==============================================================================
  - regex: ';credjaoa(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|ja-alaska.org|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Kijik Corporation
  # ==============================================================================
  - regex: ';credkc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|kijikcorp.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # KLEBS Heating & Mechanical
  # ==============================================================================
  - regex: ';credklebs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|klebsheating.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # O'Banion Real Estate & Relocation Services
  # ==============================================================================
  - regex: ';credorers(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "fn|obanionrelocation.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"

  # ==============================================================================
  # Pioneer Peak Orthopedic Surgery
  # ==============================================================================
  - regex: ';credppos(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
    vars:
      - name: client_config
        type: echo
        params: { echo: "filn|pioneerpeak.com|" } # EDIT THIS LINE
      - *credential_output
      - *temporary_password
    replace: "{{credentials}}\n{{temppass}}"
