# espanso match file

################################################################################
#======================== TEMPLATE FOR NEW CLIENTS =============================
################################################################################

# # ==============================================================================
# # Imaginary Domain
# # ==============================================================================
#
# - regex: ';credid(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.' # EDIT THIS LINE
#   vars:
#     - name: client_config
#       type: echo
#       params: { echo: "fnln|imaginarydomain.org|" } # EDIT THIS LINE
#     - *credential_output
#     - *temporary_password
#   replace: "{{credentials}}\n{{temppass}}"

################################################################################

# ==============================================================================
# CREDENTIAL MATCHES
# ==============================================================================
#
# Each client uses one client_config value:
#   username_format|email_domain|optional_email_local_format
#
# Examples:
#   fnln|client.org|          = johnsmith / johnsmith@client.org
#   fnli|client.org|fn.ln     = johns / john.smith@client.org
#
# Leave the third field blank to use the username format for the email address.
# This is the standard use, but optionally allows the email username to be
# overwritten.
#
# Format shortcuts:
#   fn    = firstname only                    (john)
#   ln    = lastname only                     (smith)
#   fnln  = firstname + lastname              (johnsmith)
#   fnli  = firstname + last initial          (johns)
#   filn  = first initial + lastname          (jsmith)
#   fili  = first initial + last initial      (js)
#   fn.ln = firstname + "." + lastname        (john.smith)
#   fn.li = firstname + "." + last initial    (john.s)
#   fi.ln = first initial + "." + lastname    (j.smith)
#   fi.li = first initial + "." + last initial (j.s)
#
# To add a client, copy an entry below and change only:
#   1. Client name comment
#   2. regex trigger line
#   3. client_config value
# ==============================================================================

anchors:
  credential_output: &credential_output
    name: credentials
    type: script
    params:
      args:
        - sh
        - "%CONFIG%/scripts/credential-generator.sh"

  temporary_password: &temporary_password
    name: temppass
    type: match
    params:
      trigger: ";tp"

matches:
  # Alaska SeaLife Center
  - regex: ';credaslc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_name" "$last_initial"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@alaskasealife.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Camp Fire Alaska Credentials ------------------------------------------------------------------
  - regex: ';credcfa(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@campfireak.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # KLEBS Heating & Mechanical ------------------------------------------------------------------
  - regex: ';credklebs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@klebsheating.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Cross Road Medical Center
  - regex: ';credcrmc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@crossroadmc.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Kijik Corporation
  - regex: ';credkc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@kijikcorp.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Junior Achievement of Alaska Credentials ------------------------------------------------------------------
  - regex: ';credjaoa(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@ja-alaska.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # O'Banion Real Estate & Relocation Services Credentials ----------------------------------------
  - regex: ';credorers(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s" "$first_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@obanionrelocation.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # - trigger: "first_initial"
  #   vars:
  #     - name: username
  #       type: shell
  #       params:
  #         cmd: |-

  # Girl Scouts of Alaska Credentials ------------------------------------------------------------------
  - regex: ';credgsak(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@girlscoutsalaska.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Pioneer Peak Orthopedic Surgery Credentials ---------------------------------------------------
  - regex: ';credppos(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@pioneerpeak.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Alaska Family Services Credentials ------------------------------------------------------------
  - regex: ';credafs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_name" "$last_initial"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@akafs.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Indian Family Health Clinic Credentials -------------------------------------------------------
  - regex: ';credifhc(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s.%s" "$first_name" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@ifhcgf.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # CCS Early Learning Credentials ---------------------------------------------------
  - regex: ';credccs(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@ccsalaska.org" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}

  # Anchorage Neurosurgical Associates, Inc.
  - regex: ';credanai(?P<firstname>[^,]+),(?P<lastname>[^.]+)\.'
    vars:
      - name: username
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}")
            last=$(echo "{{lastname}}")

            first_initial=$(echo "$first" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            last_initial=$(echo "$last" | awk '{ for (i=1; i<=NF; i++) printf tolower(substr($i,1,1)) }')
            first_name=$(echo "$first" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
            last_name=$(echo "$last" | tr '[:upper:]' '[:lower:]' | tr -d ' ')

            printf "%s%s" "$first_initial" "$last_name"
      - name: fullname
        type: shell
        params:
          cmd: |-
            first=$(echo "{{firstname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            last=$(echo "{{lastname}}" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
            printf "%s %s" "$first" "$last"
      - name: email
        type: shell
        params:
          cmd: |-
            printf "%s@akneurosurgery.com" "{{username}}"
      - name: temppass
        type: match
        params:
          trigger: ";tp"
    replace: |-
      {{fullname}}
      Username: {{username}}
      Email: {{email}}
      {{temppass}}
